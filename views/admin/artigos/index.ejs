<%- include('../../partials/header') %>
    <%- include('../../partials/navbar') %>

        <body>
            <div class="container">
                <% artigos.forEach(artigo => {  %>
                    <div class="card">
                        <div class="card-header">
                            <h5>
                                <%= artigo.titulo %>
                            </h5>
                        </div>
                        <div class="card-body">
                            <a href="/admin/artigo/<%= artigo.id_artigo %>" class="btn btn-success">Ler artigo</a>
                        </div>
                    </div>
                    <br>
                    <% }) %>

            </div>
        </body>
        <%- include('../../partials/footer') %>